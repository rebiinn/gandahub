# Used when this service’s Root Directory in Railway is set to `frontend`.
# Docker avoids Nixpacks “build plan” failures for this Vite + monorepo setup.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
